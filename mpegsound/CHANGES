The only changes made by the author of MP3Blaster are one variable made protected i/o private in some class.
And a fix in mpegtoraw.cc that makes the YEAR-record in the ID3-tag of an mp3 4 bytes long (i/o 7):

--- mpegsound.h.orig	Fri Mar  6 17:43:55 1998
+++ mpegsound.h	Mon Jan 18 23:35:01 1999
@@ -691,6 +691,8 @@
   
 private:
   Soundinputstream *loader;
+
+protected:
   Wavetoraw *server;
 };
 
@@ -712,6 +714,8 @@
 
 private:
   Soundinputstream *loader;
+
+protected:
   Mpegtoraw *server;
 
   void showverbose(int verbose);
--- mpegtoraw.cc.orig	Fri Mar  6 17:43:55 1998
+++ mpegtoraw.cc	Mon Nov 23 12:54:03 1998
@@ -198,7 +198,7 @@
 	  fp->_readbuffer(data->songname,30);strman(data->songname,30);
 	  fp->_readbuffer(data->artist  ,30);strman(data->artist,  30);
 	  fp->_readbuffer(data->album   ,30);strman(data->album,   30);
-	  fp->_readbuffer(data->year    , 7);strman(data->year,     7);
+	  fp->_readbuffer(data->year    , 4);strman(data->year,     4);
 	  fp->_readbuffer(data->comment ,30);strman(data->comment, 30);
           break;
         }
